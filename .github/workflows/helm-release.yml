name: Release Helm Chart

on:
  push:
    branches:
      - main
    paths:
      - "charts/**"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  helm:
    uses: czerwonk/go-ci/.github/workflows/helm-release.yml@v2
    with:
      chart-path: charts/bird-exporter
      pages-url: https://czerwonk.github.io/bird_exporter
